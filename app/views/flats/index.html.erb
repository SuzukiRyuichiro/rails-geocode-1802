<p style="color: green"><%= notice %></p>

<h1>Flats</h1>

<!--
<div id="flats">
  <% @flats.each do |flat| %>
    <%= render flat %>
    <p>
      <%= link_to "Show this flat", flat %>
    </p>
  <% end %>
</div>
-->
<%= link_to "New flat", new_flat_path %>

<div data-map-mapbox-token-value="<%= ENV['MAPBOX_TOKEN'] %>" style="width: 1080px; height: 720px;" data-controller="map"></div>
